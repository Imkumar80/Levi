[project]
name = "levi"
version = "0.1.0"
description = "LEVI V0 - Working Agent Loop"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.45.0",
    "google-genai>=0.3.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
